<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.UIDesign"> <!-- UI 프로젝트의 패키지 이름 -->

    <!-- ▼▼▼ 1. 필요한 권한들을 모두 선언합니다 ▼▼▼ -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- (중요!) 안드로이드 10 (Q) 미만 기기에서 갤러리에 저장할 때 필요 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <!-- (필수!) 저장된 사진을 다시 읽어와서 서버로 보낼 때 필요 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- (필수!) Flask 서버와 통신할 때 필요 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲ -->
    <!--- For Android 13 and above -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- 알람기능 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Camera"
        android:requestLegacyExternalStorage="true"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"> <!-- http 통신 허용 -->


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Camera">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ▼▼▼ 2. 모든 Activity들이 등록되어 있는지 확인 ▼▼▼ -->
        <activity android:name="com.example.camera.CameraActivity" />
        <activity android:name="com.example.db.StatisticsActivity" />
        <activity android:name="com.example.camera.AlarmSettingsActivity" />
        <activity android:name="com.example.camera.RecyclingResultActivity"/>
        <activity android:name="com.example.camera.RecycleTipActivity"/>
        <activity android:name="com.example.db.ProfitReportActivity"/>
        <activity android:name="com.example.guideline.MistakeGuideActivity"/>
        <!-- ▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲ -->

        <receiver
            android:name="com.example.alarm.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name="com.example.camera.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>



    </application>

</manifest>

